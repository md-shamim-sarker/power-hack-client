import React from 'react';

const Header = () => {
    return (
        <div>
            <h2 className="text-2xl">This is header component.</h2>
        </div>
    );
};

export default Header;